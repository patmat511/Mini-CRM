{% extends 'base.html.twig' %}

{% block title %}Stage{% endblock %}

{% block body %}
    <h1>Stage</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>StageId</th>
                <td>{{ stage.stageId }}</td>
            </tr>
            <tr>
                <th>StageName</th>
                <td>{{ stage.stageName }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_stage_index') }}">back to list</a>

    <a href="{{ path('app_stage_edit', {'stageId': stage.stageId}) }}">edit</a>

    {{ include('stage/_delete_form.html.twig') }}
{% endblock %}
