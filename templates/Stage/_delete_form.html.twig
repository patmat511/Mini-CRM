<form method="post" action="{{ path('app_stage_delete', {'stageId': stage.stageId}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ stage.stageId) }}">
    <button class="btn">Delete</button>
</form>
