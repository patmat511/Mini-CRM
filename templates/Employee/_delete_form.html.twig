<form method="post" action="{{ path('app_employee_delete', {'employeeId': employee.employeeId}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ employee.employeeId) }}">
    <button class="btn">Delete</button>
</form>
