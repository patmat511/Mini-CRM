{% extends 'base.html.twig' %}

{% block title %}Roles index{% endblock %}

{% block body %}
    <h1>Roles index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>RoleId</th>
                <th>RoleName</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for role in roles %}
            <tr>
                <td>{{ role.roleId }}</td>
                <td>{{ role.roleName }}</td>
                <td>
                    <a href="{{ path('app_roles_show', {'roleId': role.roleId}) }}">show</a>
                    <a href="{{ path('app_roles_edit', {'roleId': role.roleId}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_roles_new') }}">Create new</a>
{% endblock %}
