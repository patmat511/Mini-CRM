<form method="post" action="{{ path('app_roles_delete', {'roleId': role.roleId}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ role.roleId) }}">
    <button class="btn">Delete</button>
</form>
